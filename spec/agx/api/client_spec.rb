require 'spec_helper'

describe Agx::Api::Client do
  it 'has a version number' do
    expect(Agx::Api::Client::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
